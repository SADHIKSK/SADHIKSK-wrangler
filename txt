## Byte Size and Time Duration Parsers

This enhancement adds support for parsing byte sizes (e.g., "10KB", "1.5MB") and time durations (e.g., "100ms", "2s") in Wrangler recipes. A new directive, `aggregate-stats`, computes aggregates (e.g., total size, total time) from columns containing these units.

### Usage

**Recipe Example**: